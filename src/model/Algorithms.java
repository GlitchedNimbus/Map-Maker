package model;
import java.util.ArrayList;
import java.util.Random;

public class Algorithms{
	
	/*will us a set of ordered pairs, minimum roll is a 5
	 * G = base connections between rooms 
	 * Takes {G(r)* U (i,j)}
	 * i and j are (as a pair)unique to the set of G
	 */

	//will add the number of edges(connections)to the base connections
	
	public void monsterMash(){
		
	}
}
